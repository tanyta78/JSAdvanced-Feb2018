/**TableArticle {
  headings,                 // table headings
  data,                     // table data
  loadData(headings, data), // set headings and data
  getElementString()        // override base method to return correct HTML
}
 */